

    <%= select_tag "main_category",options_for_select(Category.roots.collect {|p| [p.name,p.id]},cur[0].id) %>
    
    <%= f.select :category_id, options_for_select(cur[0].children.collect {|p| [p.name,p.id]},cur[1].id),{},:class=>'subcategory' %>
   