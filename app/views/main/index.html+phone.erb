  <%=content_tag :div,id: mobile_page_id, data: {role: 'page'} do %>

  <%=render 'shares/header'%>
  
    <div data-role="content">
	
		
		<ul data-role='listview' data-inset='true'>
	        <% Lesson.local_lessons(get_city).order("rank desc").limit(10).each do |course| %>
              <li> 
				  <h3><%= link_to display_string(course.title,23),lesson_path(course) %></h3>
	              <p><%=  display_string(course.company_name,23) %></p>
	              <span class="ui-li-aside"><%= course.rank %></span>
			  </li>
			<% end %>
		</ul>
		
    </div>
  
  <%=render 'shares/footer'%>
    
 

 <% end %>
 
 <%=render 'shares/category_page'%>