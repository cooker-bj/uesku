$("ul[data-role='listview'].lists_phone").append('<%=j render 'list_items' %>').listview('refresh');
<% if @places.next_page %>
	$('.pagination').replaceWith('<%= j will_paginate @places %>');
<% else %>
	$(document).off('scroll');
	$('.pagination').remove();
<% end %>
