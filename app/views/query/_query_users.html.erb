<ul>
	<% @users.each do |user| %>
		<li>
			<%=link_to user.name, profile_user_path(user)%>
			<%=link_to '加为好友',friendships_path(:friend_id=>user.id),:method=>'post',:class=>'add_friend', :remote=>true unless user==current_user%>
		</li>

	<% end %>
</ul>