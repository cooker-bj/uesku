<%= form_for @post,:remote=>true do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title,'标题' %><br />
    <%= f.text_field :title %>
  </div>
  
  <div class="field">
   
    <%= f.text_area :content,:id=>'comment_editor' %>
  </div>
  <div class="actions">
    <%= f.submit '更新' %>
  </div>
<% end %>
