$("ul.comment_items_phone").append('<%=j render 'comment_lists' %>').trigger('create');
<% if @comments.next_page %>
	$('.pagination').replaceWith('<%= j will_paginate @comments %>');
<% else %>
	$(document).off('scroll');
	$('.pagination').remove();
<% end %>